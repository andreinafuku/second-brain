
Modelo ReAct - Reasoning Act
Primeio pensa e depois decide qual ação tomar

## Vantagens
- precisao
- facilidade
- versatilidade
- eficiencia


promt

Hoje no fluxo de caixa, ao chamarmos o endpoint /api/tesouraria/visoes-fluxos-caixa/${visaoId}/dados ele está trazendo todos os dados referente às dimensões de uma única vez, o que prejudica a performance ao carregar o grid. A ideia é que a API possua query params, para que eu possa passar o dimLin1, dimLin2... etc de acordo com o fluxo definido pelo usuário, ex: dimLin1 sendo conta financeira 001, seria possivel no front end passarmos os query params para API por meio do clique no #file:ExpandButtonRenderer.vue ? assim vamos populando as células de acordo com o clique do usuário, inicialmente ele vai trazer os dados consolidados pela primeira dimensão (visivel) e ao clicar nos botões de expansão passaríamos o parametro pra API popular o grid, isso seria possivel? como poderíamos implementar no front e backend?